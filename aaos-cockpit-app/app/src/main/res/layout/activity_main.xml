<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="#101418">

    <!-- On-device MediaPipe FaceLandmarker/HandLandmarker camera feed. Visible (rather
         than headless) so judges can see the driver-facing feed alongside the cockpit
         status during the demo, per root CLAUDE.md's Demo Script. Only populated in
         DetectionBackendMode.LIVE_CAMERA; stays empty/hidden in REPLAY_FILE mode. -->
    <androidx.camera.view.PreviewView
        android:id="@+id/cameraPreview"
        android:layout_width="320dp"
        android:layout_height="240dp"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/statusText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/cockpit_status_normal"
        android:textColor="#FFFFFF"
        android:textSize="28sp" />

    <!-- Mandatory debug overlay (root CLAUDE.md "Debug Overlay" section) —
         surfaces perclos/eyeOpenProbability/headEulerAngleX/receivingTrigger/
         lastGatewayAction live so the model/FSM/gateway layers can be told
         apart without reading logcat. -->
    <TextView
        android:id="@+id/overlayPerclos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#B0B8C0"
        android:textSize="16sp"
        android:layout_marginTop="16dp" />

    <TextView
        android:id="@+id/overlayEyeOpen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#B0B8C0"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/overlayHeadPitch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#B0B8C0"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/overlayReceiving"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#B0B8C0"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/overlayGatewayAction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#B0B8C0"
        android:textSize="16sp" />

</LinearLayout>
